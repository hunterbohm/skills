interface:
  display_name: "Operations Audit"
  short_description: "Consent-based operations diagnosis roadmap"
  default_prompt: "Use $ops-audit to diagnose my operations and render an offline roadmap."
policy:
  allow_implicit_invocation: false

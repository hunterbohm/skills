interface:
  display_name: "Operations Audit"
  short_description: "Consent-based time-sink audit the agent then does"
  default_prompt: "Use $ops-audit to find my time sinks and read the plan back in chat."
policy:
  allow_implicit_invocation: false
